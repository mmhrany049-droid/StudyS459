# تصمیم‌های باز — نسخه ۱

عامل نباید این موارد را حدس بزند:

1. فرمول دقیق درصد و نمره منفی (در نسخه ۱ فقط correct/wrong/unanswered استفاده شود).
2. فرمول Mastery (به نسخه ۲ منتقل شده).
3. الگوریتم Spaced Repetition (به نسخه ۲ منتقل شده).
4. سیاست حذف سؤال‌های اخیر از random pool (فعلاً فقط no-duplicate داخل session).
5. ساعت‌بندی دقیق داخل یک روز (کاربر placement را کنترل می‌کند).
6. مدل نمره‌دهی انواع امتحان.
7. trigger و ساعت دقیق Telegram (چون اختیاری است).
8. فرمت نهایی package import (فعلاً JSON ساده).
9. روش نهایی authentication (ساده نگه داشته شود).
10. وزن نتایج امتحان در تحلیل‌ها.

معماری باید برای تغییر این موارد در نسخه‌های بعدی آماده باشد.
