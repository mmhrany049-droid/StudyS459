"""External integrations — Telegram ONLY, optional, never required (spec 00).

Telegram must only consume core services; it must never duplicate logic.
Added only after a stable v1, and only on explicit request (spec 16).
"""
