# API دلتا — نسخه ۲.۲

## بانک تست
GET /books/{id}/nodes/{node_id}/questions  
PUT /books/{id}/nodes/{node_id}/questions/bulk  
PATCH /questions/{id}  
POST /books/{id}/nodes/{node_id}/questions/range  {from, to}

## ورود گذشته
POST /attempts/import-by-book  
{ book_id, answers: [{ sequence_ref یا question_id, choice: 1|2|3|4|null }] }

## تدریس‌شده
GET/POST /taught-topics  
GET /taught-topics/insights

## امتحان
GET/POST /exams  
POST /exams/{id}/files  
PUT /exams/{id}/answer-key  
POST /exams/{id}/attempts  
GET /exams/{id}/attempts

## آزمایشی
PUT /exams/{id}/sections  
PUT /exams/{id}/topic-map

## آمادگی
GET/POST /upcoming-exams  
GET /upcoming-exams/{id}/suggested-tasks
