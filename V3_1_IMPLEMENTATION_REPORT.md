# گزارش اجرای V3.1 — ارتقای StudyS459

> مبنای کار: بستهٔ `study_system_v3_1_docs/` (۰۰ تا ۱۱) روی کد V3 موجود.
> هیچ‌چیز از صفر بازنویسی نشد؛ همهٔ تغییرها **افزایشی** هستند و داده/تاریخچه پاک نمی‌شود.
> تاریخ اجرا: ۱۴۰۵/۰۶/۲۷ — نسخهٔ مدل: `v3.1.0`

---

## ۱. خط تیره‌ها (کامیت‌ها)

| کامیت | فاز | عنوان |
|---|---|---|
| `4bbfcc1` | ۱ | Audit matrix، مهاجرت افزایشی schema، بازگردانی درخت شیمی |
| `4299d0e` | ۲ | سیستم یکپارچهٔ آزمون + Exam Center |
| `df6ffb5` | ۳ | چکاپ شیمی به‌صورت «بازهٔ پوشش» |
| `25759b1` | ۴ | درخت دهم–دوازدهم + قانون «plannable فقط با بانک تست» |
| `9492ce4` | ۵ | تقویم شمسی ۱۴۰۵–۱۴۰۸ |
| `7b876c9` | ۶ | انواع کار مطالعهٔ قابل گسترش |
| `4255c34` | ۷ | پرسش‌گری هدف‌دار متصل به capacity و planner |
| `f9b7a90` | ۸ | «امروز» داشبورد + تقویم هفته + نسخهٔ ۳.۱ |
| `bac15a3` | ۸ | رجیستری فعالیت‌ها + تست‌های پذیرش فاز ۸ |
| `0ef43df` | اصلاح | آمادگی آزمون بدون شاهد، صفر گزارش نمی‌شود + اسکریپت پذیرش V3.1 |

## ۲. وضعیت تست‌ها

- `pytest app/tests/` → **۸۱ تست موفق**، ۰ شکست.
- تست‌های پذیرش فازها در `backend/app/tests/test_v3_1_phases.py` (۲۱ تست):
  فاز ۱: ۳ | فاز ۲: ۴ | فاز ۳: ۲ | فاز ۴: ۲ | فاز ۵: ۲ | فاز ۶: ۱ | فاز ۷: ۳ | فاز ۸: ۴.
- `python backend/acceptance_v3_1.py` → **۱۱ از ۱۱ آیتم پذیرش سند ۱۱** (دریچهٔ انتشار).
- `python backend/e2e_smoke.py` → بدون شکست (حلقهٔ INPUT→…→REPLAN).
- فرانت: `npx tsc -b` تمیز، `npm run build` → ۳۰۰.۰۵ کیلوبایت (gzip ۸۷.۷۲).

---

## ۳. گزارش هر فاز

### فاز ۱ — Audit و بازگردانی
**تغییر:** پر کردن ماتریس `02_AUDIT_MATRIX_V3_TO_V3_1.md` با کد واقعی؛ پارسر افزایندهٔ
فهرست شیمی (فرمت قدیم + جدید)؛ مهاجرت افزایشی `sync_schema` (فقط `ALTER TABLE ADD COLUMN`،
idempotent) که در `init_db()` صدا زده می‌شود.
**نتیجهٔ اندازه‌گیری‌شده:** شیمی ۲ → `{chapters: 3, sections: 47, subsections: 57, leaves: 92}` با
۱۷ نشانهٔ چکاپ/جامع (پیش از این: صفر گره)؛ همهٔ کتاب‌ها ≥۳ فصل و ≥۲۰ برگ.
**API/صفحات:** `backend/app/db/{curriculum_outline,migrations}.py`، `Curriculum.tsx`.

### فاز ۲ — سیستم آزمون یکپارچه + Exam Center
**تغییر:** `ExamType` پنج‌گانه (`personal|school|mock|checkup|comprehensive`)، چند درس
(`subjects[]`)، پاسخ‌نامهٔ آزمون (`seq→1..4`)، نوبت‌ها (`attempts[]`)، آپلود فایل،
تیک مبحث با cascade، برنامهٔ آماده‌سازی چندروزه، و Exam Center «گذشته | آینده | تحلیل».
**API:** `POST/PATCH/GET /exams…`, `/exams/{id}/topics`, `/exams/{id}/attempts`,
`/exams/{id}/retake`, `/exams/{id}/files`, `/exam-center`, `/exams/prep-plan/{id}`.
**صفحات:** `Exams.tsx`.
**پذیرش:** ۴ تست فاز ۲ — پنج نوع، `422` برای نوع/درس نامعتبر، ورود فشردهٔ پاسخ‌نامه
(`"1:2,2:3,3:1,4:4,5:0"` → ۴ کلید + ۱ پاک‌شده + `recalc_required`)، تلاش
(`correct2/wrong1/unanswered1/not_entered2`)، retake، prep-plan.

### فاز ۳ — چکاپ شیمی = بازهٔ پوشش
**تغییر:** مدل `CheckupCoverage` با هویت `book_id + order_index` (برچسب تکرارشدنی است)،
ساخت جلسه از بازه با clamp هر مبحث (`skipped_topics`)، لینک به آزمون نوع `checkup`.
**نتیجه:** ۱۷ بازه (۱۶ بازهٔ چندمبحثی + ۱ تک‌مبحثی)؛ جلسهٔ «چکاپ اول» → ۴ مبحث، ۱۲ سؤال،
برآورد پله‌ای ۲۲–۵۰ دقیقه؛ اتصال آزمون → ۴ مبحث علامت‌خورده با تاریخ «۱۴۰۵/۰۸/۱۰».
**API:** `GET /exam-checkups`, `GET /exam-checkups/{id}`, `POST …/session`, `POST …/exam`.

### فاز ۴ — درخت دهم–دوازدهم و plannable
**تغییر:** ۷ درس و ۱۱ کتاب پایهٔ ۱۰–۱۲؛ مبحث بدون بانک تست «فقط نمایشی» است و در
پیشنهاد/برنامه نمی‌آید؛ خروجی‌ها دلیل می‌دهند (`plannable_reason`, `excluded_without_bank`).
**API:** `GET /curriculum/overview`، `/books/{id}/tree` (فیلدهای `plannable`)، `/api/priorities`
(`visible_only*`).
**صفحات:** `Curriculum.tsx` (تب پایه‌ها + نشان «قابل برنامه‌ریزی/فقط نمایشی»).
**نتیجه:** ۱۴ کتاب — دهم ۳، یازدهم ۵، دوازدهم ۶؛ `visible_only_total=233`.

### فاز ۵ — تقویم شمسی ۱۴۰۵–۱۴۰۸
**تغییر:** سرویس تقویم (ماه/سال/روز/هفته)، تعطیلات ثابت شمسی به‌صورت داده با ذکر منبع،
مناسبت‌های شخصی (`CalendarOccasion`)، رویدادهای روز از آزمون/کار/فعالیت/هدف.
مناسبت قمری **حدس زده نمی‌شود**؛ خود کاربر اضافه می‌کند. بازه از config می‌آید و بیرون آن `422`.
**API:** `/calendar/range|month|year|day|week|occasions`.
**صفحات:** `Calendar.tsx` (شبکهٔ شنبه‌محور + تعطیلات + رویدادها + فرم مناسبت)،
`JalaliDateInput` بازهٔ ۱۴۰۵–۱۴۰۸ را پوشش می‌دهد؛ هیچ تاریخ میلادی در UI نیست.
**پذیرش:** ۲ تست — کبیسه ۱۴۰۸ (اسفند ۳۰ روز)، ۱۴۰۵ (۲۹)، اول فروردین تعطیل، `1409 → 422`.

### فاز ۶ — انواع کار مطالعهٔ قابل گسترش
**تغییر:** رجیستری `domain/task_types.py` با ۷ نوع پایه (`study, practice_test, review,
exam_analysis, notes_completion, written_practice, other`)، ۸ کد قدیمی V3 که **معتبر و map** شده‌اند
(بدون بازنویسی ردیف‌ها) و قلاب `tasks.custom_types` برای افزودن از config.
**API:** `GET /tasks/types`؛ نوع نامعتبر → `422` با فهرست انتخاب‌های معتبر (بدون «سایر» بی‌صدا).
**صفحات:** کارت «افزودن کار مطالعه» در `Planner.tsx` که فهرست نوع‌ها را از سرور می‌گیرد.

### فاز ۷ — پرسش‌گری هدف‌دار (سند ۰۷)
**تغییر:** چهار کانال (`onboarding`, `day_start`, `day_end`, `weekly`) با بودجهٔ ۲–۴ سؤال،
انتخاب تطبیقی بر پایهٔ «ارزش اطلاعاتی»، دلیل «چرا می‌پرسم؟» روی هر سؤال، رد کردن به‌عنوان
پاسخ معتبر، و بدون پرسیدن سؤال تکراری در همان روز.
هر پاسخ یک اثر **کوچک و باندشده** دارد: انرژی/وقت آزاد/خواب/استرس → ظرفیت همان روز،
اجرا و مانع → برآورد فردا، بار/تمرکز هفته → وزن هدف و مرور. سقف: هر پاسخ ۱۰٪، مجموع روز ۱۵٪،
وزن ۱۵٪ — همه در `questioning.*` قابل تغییر.
پاسخ خام در `DailyCheckin/WeeklyReflection` می‌ماند و اثر مشتق در جدول افزایندهٔ `question_effects`
ذخیره می‌شود (خام ≠ مشتق).
**API:** `/questioning/channels|daily|ordering|weights`، `/checkins/questions`، `/reflections/questions`.
**صفحات:** `CheckinCard.tsx` در «امروز» (آغاز/پایان روز + اثرها)، یادداشت اصلاح ظرفیت در کارت
ظرفیت، کارت «از پاسخ‌های خودت» در `Planner.tsx`.
**پذیرش (اندازه‌گیری‌شده):** یک پاسخ ≤۰.۱۰؛ گزارش کم‌انرژی ظرفیت ۱۶۵→۱۴۰ دقیقه (فاکتور ۰.۸۵،
سقف ۱۵٪)؛ ظرفیت هرگز بالای وقت آزاد یا زیر ۳۰ دقیقه نمی‌رود؛ پایان روز → فردا (اجرا ۲ → −۳٪،
گوشی → −۲٪)؛ بار هفتگی «سنگین» → وزن فرصت −۵٪؛ ترجیح شخصیتی فقط با ≥۵ شاهد و اطمینان ≥۰.۳۵ و
فقط روی **ترتیب** پیشنهادهای هم‌امتیاز اثر می‌گذارد (هیچ قفلی وجود ندارد).

### فاز ۸ — داشبورد و ظاهر (سند ۰۸)
**تغییر:** داشبورد به ترتیب سند: سلام → امروز → ۳ کار مهم → اولویت → آزمون نزدیک →
وضعیت آمادگی → پیشنهاد بعدی (`today_brief`)، بدون محاسبهٔ دوباره در UI؛ آزمون‌های آینده
`readiness` + `next_action` (علامت‌زدن مبحث / تمرین کوتاه / تمرین متمرکز / مرور) را با خود می‌آورند؛
`/planning/week` تقویمی شد (شنبه→جمعه با تعطیلات و رویدادها)؛ کارت فعالیت‌ها در تنظیمات.
**صفحات:** `Dashboard.tsx`، `Planner.tsx`، `Settings.tsx`.
**اصلاح مهم:** آمادگی آزمون فقط از مباحث **شاهددار** ساخته می‌شود؛ تیک فصل بدون تمرین
«۰٪» گزارش نمی‌شود بلکه «نامعلوم + دلیل» است (خام ≠ مشتق، بدون دقت کاذب).
**نسخه:** `MODEL_VERSION = v3.1.0` و نمایش نسخهٔ در حال اجرا در هدر از `/api/health`.

---

## ۴. پذیرش سند ۱۱ (اجرای واقعی روی دیتابیس تازه)

| # | آیتم | نتیجه |
|---|---|---|
| ۱ | Exam با typeهای ۵گانه | ✅ «جامع»، ۵ نوع |
| ۲ | تیک فصل روی Exam فرزندان را علامت می‌زند | ✅ ۱ فصل → ۴۰ مبحث |
| ۳ | Checkup چند مبحث را پوشش می‌دهد | ✅ ۴ مبحث، ۹ سؤال، ۳ مبحث سؤال‌دار |
| ۴ | Curriculum ۱۰–۱۲ + plannable فقط با بانک | ✅ دهم ۳/یازدهم ۵/دوازدهم ۶، `visible_only_total=233` |
| ۵ | تقویم شمسی ۱۴۰۵–۱۴۰۸ | ✅ بازه ۱۴۰۵–۱۴۰۸، اول هفته شنبه |
| ۶ | Exam Center گذشته/آینده | ✅ گذشته با ۱ تلاش، آینده با ۴۰ مبحث و آمادگی نامعلوم |
| ۷ | Retake تاریخچه را پاک نمی‌کند | ✅ نوبت ۱ (۶۲.۵٪) دست‌نخورده، نوبت جدید ۲ |
| ۸ | بانک تست + پاسخ‌نامه per مبحث | ✅ ورود فشرده ۵ کلید + بازهٔ ۱۲ سؤالی |
| ۹ | Import گذشته با «نزده» | ✅ ۱ فصل، ۳۲ ردیف در فهرست بزرگ، ثبت موفق |
| ۱۰ | سؤال آغاز روز روی بار امروز اثر دارد | ✅ ۱۶۵→۱۴۰ دقیقه (−۱۵٪) با ۲ دلیل |
| ۱۱ | رگرسیون قابلیت‌های سالم V3 | ✅ داشبورد + ۱۵ مرحلهٔ برنامه‌ریزی + تقویم هفته |

**نتیجه: ۱۱/۱۱** — دستور اجرا: `STUDYS459_DB_PATH=/tmp/acceptance.db python backend/acceptance_v3_1.py`

## ۵. رعایت قواعد سخت V3/V3.1

- تدریس‌شده ≠ یادگرفته‌شده (تیک فقط صلاحیت تمرین را عوض می‌کند).
- فعالیت ≠ کار مطالعه (فعالیت ظرفیت می‌گیرد، هیچ‌وقت «کار انجام‌نشده» نمی‌شود).
- اولویت ≠ برنامه ≠ پیشنهاد؛ توصیهٔ پذیرفته‌شده معیار کیفیت نیست.
- سؤال ≠ پاسخ‌نامه ≠ پاسخ‌برگ؛ «نزده» ≠ «ثبت‌نشده» ≠ «غلط».
- دادهٔ خام ≠ دادهٔ مشتق (اثر پاسخ‌ها در جدول جدا؛ همه‌چیز بازساختنی).
- یک خطا ≠ ضعف؛ یک موفقیت ≠ تسلط؛ بدون دقت کاذب؛ بدون «هوش مصنوعی گفته».
- هیچ عدد پژوهشی هارد‌کد نشد: همه در رجیستری پارامترها با منبع/اطمینان (PARAMS).
- UI فقط شمسی، RTL؛ هیچ شماره/تاریخ میلادی در رابط نیست.
- تلگرام همچنان اختیاری و non-blocking است.

## ۶. کارهای باقی‌مانده (شفاف)

- پر کردن فهرست کتاب‌های دوازدهم/دهم از فایل‌های فهرست کاربر (ساختار آماده، محتوا دست کاربر است).
- نمایش تایم‌لاین ساعتی در برنامهٔ هفته (اکنون نوار تقویمی + فهرست روزها).
- اتصال مناسبت‌های قمری فقط از راه ورود کاربر (به‌عمد حدس نمی‌زنیم).
