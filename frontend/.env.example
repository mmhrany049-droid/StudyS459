# Leave empty: the app uses relative URLs and the Vite dev server proxies
# API paths to the backend (no CORS issues, preview-safe).
# Set only if the frontend must talk to a backend on another origin directly.
VITE_API_URL=
# Backend target for the Vite dev proxy (server-side, not the browser).
VITE_BACKEND_URL=http://localhost:8000
